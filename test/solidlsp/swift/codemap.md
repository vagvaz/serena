# test/solidlsp/swift/

## Responsibility
Tests for the Swift language server integration.

## Test Approach
Uses `test_swift_basic.py` with parametrized `language_server` fixture. Validates basic symbol resolution for Swift source files.

## Markers
`@pytest.mark.swift`
