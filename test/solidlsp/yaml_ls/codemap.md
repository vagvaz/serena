# test/solidlsp/yaml_ls/

## Responsibility
Tests for the YAML language server integration.

## Test Approach
Uses `test_yaml_basic.py` with parametrized `language_server` fixture. Validates basic symbol resolution for YAML files.

## Markers
`@pytest.mark.yaml`
