# test/solidlsp/dart/

## Responsibility
Tests for the Dart language server integration.

## Test Approach
Uses `test_dart_basic.py` with parametrized `language_server` fixture. Validates basic symbol tree and document symbol functionality for Dart source files.

## Markers
`@pytest.mark.dart`
