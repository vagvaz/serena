# test/solidlsp/crystal/

## Responsibility
Tests for the Crystal language server integration.

## Test Approach
Uses `test_crystal_basic.py` with parametrized `language_server` fixture. Validates basic symbol resolution and language server operations for Crystal source files.

## Markers
`@pytest.mark.crystal`
