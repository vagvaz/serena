# test/solidlsp/lua/

## Responsibility
Tests for the Lua language server integration.

## Test Approach
Uses `test_lua_basic.py` with parametrized `language_server` fixture. Validates basic symbol resolution for Lua source files.

## Markers
`@pytest.mark.lua`
