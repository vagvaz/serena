# test/solidlsp/markdown/

## Responsibility
Tests for the Markdown language server integration.

## Test Approach
Uses `test_markdown_basic.py` with parametrized `language_server` fixture. Validates basic symbol resolution for Markdown documents.

## Markers
`@pytest.mark.markdown`
