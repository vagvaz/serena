# test/solidlsp/al/

## Responsibility
Tests for the AL (AppLanguish / AL Language) language server integration.

## Test Approach
Uses `test_al_basic.py` with parametrized `language_server` fixture. Validates basic symbol retrieval and server initialization.

## Markers
`@pytest.mark.al`
