# test/solidlsp/perl/

## Responsibility
Tests for the Perl language server integration.

## Test Approach
Uses `test_perl_basic.py` with parametrized `language_server` fixture. Validates basic symbol resolution for Perl source files.

## Markers
`@pytest.mark.perl`
