# test/solidlsp/solidity/

## Responsibility
Tests for the Solidity language server integration.

## Test Approach
Uses `test_solidity_basic.py` with parametrized `language_server` fixture. Validates basic symbol resolution and server communication for Solidity source files.

## Markers
`@pytest.mark.solidity`
