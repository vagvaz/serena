# test/solidlsp/matlab/

## Responsibility
Tests for the MATLAB language server integration.

## Test Approach
Uses `test_matlab_basic.py` with parametrized `language_server` fixture. Validates basic symbol resolution for MATLAB source files.

## Markers
`@pytest.mark.matlab`
