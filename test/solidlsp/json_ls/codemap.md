# test/solidlsp/json_ls/

## Responsibility
Tests for the JSON language server integration.

## Test Approach
Uses `test_json_basic.py` with parametrized `language_server` fixture. Validates basic symbol resolution for JSON documents.

## Markers
`@pytest.mark.json`
