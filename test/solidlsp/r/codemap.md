# test/solidlsp/r/

## Responsibility
Tests for the R language server integration.

## Test Approach
Uses `test_r_basic.py` with parametrized `language_server` fixture. Validates basic symbol resolution for R source files.

## Markers
`@pytest.mark.r`
