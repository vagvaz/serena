# test/solidlsp/fsharp/

## Responsibility
Tests for the F# language server integration.

## Test Approach
Uses `test_fsharp_basic.py` with parametrized `language_server` fixture. Validates basic symbol resolution and language server operations for F# source files.

## Markers
`@pytest.mark.fsharp`
